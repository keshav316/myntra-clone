Interval(() => {
//   counter = (counter + 1) % totalSlides; // Wrap around using modulo
//   slideImage();
// }, 3000); // 3000 milliseconds = 3 seconds

// const goNext = () => {
//   counter = (counter + 1) % totalSlides; // Wrap around using modulo
//   slideImage();
// };

// const goPrev = () => {
//   counter = (counter - 1 + totalSlides) % totalSlides; // Wrap around and handle negative counter
//   slideImage();
// };
